# wxHexEditor

# Compilation dependencies on Termux

Run 'pkg install wxwidgets';

# Make sure you have 'make' installed, on your termux.

# If not, Run 'pkg install make', then navigate to the wxHexEditor directory.

# Compilation instructions
Just launch `make`!
